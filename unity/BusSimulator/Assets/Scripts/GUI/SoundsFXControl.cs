using UnityEngine;
using System.Collections;

public class SoundsFXControl : MonoBehaviour {

	/**
	 * Author: Flavian Popa
	 * 
	 **/

	public GameObject hornMusic;
	public GameObject doorsMusic;
	
	// Update is called once per frame
	void Update () {
		//Modify with the correct buttons
		/*if(Input.GetKey(KeyCode.Space)) {
			hornMusic.GetComponent<AudioSource> ().Play ();
		}
		if(Input.GetKey(KeyCode.Space)) {
			doorsMusic.GetComponent<AudioSource>().Play();
		}*/
	}
}
